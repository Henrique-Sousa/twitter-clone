# Twitter Clone Front End

## Instructions to run locally

rename `.env.sample` to `.env`
```bash
# install dependencies
$ npm i

# build the frontend 
$ npm run build

# start the frontend 
$ serve build
```
